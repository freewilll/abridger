#!/usr/bin/env python

import argparse
from signal import signal, SIGPIPE, SIG_DFL
import sys
from minime.db_conn import DbConn
from minime.schema import Schema


def main():
    parser = argparse.ArgumentParser(
        description='Dump relations from a database')
    parser.add_argument(
        '-d', '--db-config-path', dest='db_config_path', nargs='?',
        help="database config path")

    args = parser.parse_args()
    dbconn = DbConn.load(args.db_config_path)
    conn = dbconn.connect()
    schema = Schema.create_from_conn(conn)
    conn.close()

    # Ignore SIG_PIPE and don't throw exceptions on it
    signal(SIGPIPE, SIG_DFL)
    schema.dump_relations(sys.stdout)


if __name__ == '__main__':
    main()
